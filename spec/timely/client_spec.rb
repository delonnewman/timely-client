# frozen_string_literal: true

RSpec.describe Timely::Client do
  it "has a version number" do
    expect(Timely::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
